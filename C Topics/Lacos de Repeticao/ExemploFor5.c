// Imprimir a soma dos números de 1 a 500.

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int numero;

    for (numero = 0; numero <= 500; numero++)
    {
        printf("Contagem: %i\n", numero);
    }

    system("pause");
    return 0;
}